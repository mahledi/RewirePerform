# Agent handoff — locked RewirePerform logo

Use the locked logo kit in this directory. Read `brand-lock.json` and `README.md` before implementation.

## Agent instruction

> Implement the official RewirePerform logo from `design/brand/logo-finalization-20260718/final/`. Use the asset mapped to the target surface in `README.md`. Preserve the exact B2 Normal R geometry and the locked colors `#2EAD89`, `#EEF0F2`, and `#0D0E12`. Do not redraw, regenerate, recolor, stretch, crop, round the App Store source, or add effects. Use the full accompanying brand name `RewirePerform`, never only `Rewire`. Verify dimensions, alpha behavior, contrast, small-size legibility, and the affected build/tests. Do not push, deploy, upload, or send without Mahle's explicit approval.

## Required reporting

The implementing agent must state:

1. which locked asset was used;
2. which files or external drafts were changed;
3. how dimensions, alpha, and visual rendering were verified;
4. what was not uploaded, sent, deployed, or published.
